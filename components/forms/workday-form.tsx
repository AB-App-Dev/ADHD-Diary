"use client";

interface WorkdayFormProps {
  sessionStart: Date;
  sessionEnd: Date;
}

export function WorkdayForm({ sessionStart, sessionEnd }: WorkdayFormProps) {
  // TODO: Implement workday form with validation like weekend form
  return <form>Workday Form (placeholder)</form>;
}
